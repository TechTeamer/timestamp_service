export declare function checkDigestFormat(format: string): boolean;
export declare function checkDigest(digest: string): boolean;
export declare function normalizeDigestFormat(format: string): string;
